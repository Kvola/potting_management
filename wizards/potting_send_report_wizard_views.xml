<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Pot Lot Wizard Form View -->
    <record id="potting_pot_lot_wizard_view_form" model="ir.ui.view">
        <field name="name">potting.pot.lot.wizard.form</field>
        <field name="model">potting.pot.lot.wizard</field>
        <field name="arch" type="xml">
            <form string="Empoter le lot">
                <sheet>
                    <div class="oe_title mb-3">
                        <h2>
                            <i class="fa fa-archive me-2 text-success"/>
                            <span>Empotage du lot</span>
                        </h2>
                    </div>
                    <group>
                        <group string="📦 Lot à empoter">
                            <field name="lot_id" readonly="1"/>
                        </group>
                        <group string="⚙️ Options">
                            <field name="create_new_container" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <group invisible="create_new_container" string="📦 Conteneur existant">
                        <field name="container_id" required="not create_new_container" 
                               options="{'no_create': True}"
                               placeholder="Sélectionner un conteneur..."/>
                    </group>
                    <group invisible="not create_new_container" string="🆕 Nouveau conteneur">
                        <group>
                            <field name="new_container_name" required="create_new_container" 
                                   placeholder="Ex: MSKU1234567"/>
                            <field name="new_container_type" required="create_new_container" 
                                   widget="radio" options="{'horizontal': true}"/>
                        </group>
                        <group>
                            <field name="new_seal_number" placeholder="N° de plomb (optionnel)"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="action_confirm" type="object" string="✅ Confirmer l'empotage" class="btn-success"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Send Report Wizard Form View -->
    <record id="potting_send_report_wizard_view_form" model="ir.ui.view">
        <field name="name">potting.send.report.wizard.form</field>
        <field name="model">potting.send.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Envoyer le rapport">
                <sheet>
                    <div class="oe_title mb-3">
                        <h2>
                            <i class="fa fa-envelope me-2 text-primary"/>
                            <span>Envoi de rapport par email</span>
                        </h2>
                    </div>
                    <group>
                        <group string="📊 Type de rapport">
                            <field name="report_type" widget="radio"/>
                        </group>
                    </group>
                    <group invisible="report_type != 'daily'" string="📅 Période">
                        <group>
                            <field name="date_from"/>
                        </group>
                        <group>
                            <field name="date_to"/>
                        </group>
                    </group>
                    <group invisible="report_type != 'ot'" string="🚚 Ordres de Transit">
                        <field name="transit_order_ids" widget="many2many_tags" 
                               options="{'color_field': 'state'}"
                               placeholder="Sélectionner les OT..."/>
                    </group>
                    <group invisible="report_type != 'summary'" string="📋 Commande">
                        <field name="customer_order_id" options="{'no_create': True}"/>
                    </group>
                    <group string="📧 Destinataires">
                        <group>
                            <field name="recipient_id" options="{'no_create': True}" 
                                   placeholder="Destinataire principal"/>
                        </group>
                        <group>
                            <field name="cc_partner_ids" widget="many2many_tags" 
                                   options="{'no_create': True}"
                                   placeholder="Personnes en copie"/>
                        </group>
                    </group>
                    <group string="📎 Options">
                        <field name="include_pdf" widget="boolean_toggle"/>
                        <field name="note" placeholder="Message personnalisé (optionnel)..." 
                               widget="text"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_send_report" type="object" string="📤 Envoyer" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Send Report Wizard Action -->
    <record id="action_potting_send_report_wizard" model="ir.actions.act_window">
        <field name="name">Envoyer un rapport</field>
        <field name="res_model">potting.send.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
