<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard Form View - Création individuelle d'OT depuis une Formule -->
    <record id="potting_create_ot_wizard_view_form" model="ir.ui.view">
        <field name="name">potting.create.ot.wizard.form</field>
        <field name="model">potting.create.ot.wizard</field>
        <field name="arch" type="xml">
            <form string="Créer un Ordre de Transit">
                <sheet>
                    <div class="oe_title mb-3">
                        <h2>
                            <i class="fa fa-truck me-2 text-primary" title="OT"/>
                            <span>Nouvel Ordre de Transit</span>
                        </h2>
                        <div class="text-muted mt-2">
                            <span>Commande: </span>
                            <field name="customer_order_id" readonly="1" class="oe_inline fw-bold"/>
                        </div>
                    </div>
                    
                    <!-- Section Formule - OBLIGATOIRE -->
                    <group string="📋 Sélection de la Formule (obligatoire)">
                        <field name="company_id" invisible="1"/>
                        <field name="formule_id" 
                               placeholder="Sélectionner une Formule validée..."
                               options="{'no_create': True, 'no_create_edit': True}"
                               class="fw-bold"/>
                        <div class="col-12" invisible="not formule_id">
                            <div class="alert alert-info py-2 mb-0">
                                <div class="row">
                                    <div class="col-4">
                                        <i class="fa fa-file-text-o me-1" title="Réf. CCC"/>
                                        <span>Réf. CCC: </span>
                                        <field name="formule_reference" class="oe_inline fw-bold"/>
                                    </div>
                                    <div class="col-4">
                                        <i class="fa fa-balance-scale me-1" title="Tonnage"/>
                                        <span>Tonnage: </span>
                                        <field name="formule_tonnage" class="oe_inline fw-bold"/>
                                        <span> T</span>
                                    </div>
                                    <div class="col-4">
                                        <i class="fa fa-cube me-1" title="Produit"/>
                                        <span>Produit: </span>
                                        <field name="formule_product_type" class="oe_inline fw-bold"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </group>
                    
                    <group>
                        <group string="📦 Informations principales">
                            <field name="customer_id" readonly="1"/>
                            <field name="campaign_id" 
                                   options="{'no_create': True}"
                                   domain="[('state', 'in', ['draft', 'active'])]"
                                   required="1"/>
                            <field name="consignee_id" 
                                   placeholder="Sélectionner un destinataire..."
                                   domain="[('is_potting_consignee', '=', True)]"
                                   context="{'default_is_potting_consignee': True}"
                                   options="{'no_quick_create': True}"/>
                            <field name="product_type" widget="radio" options="{'horizontal': true}" readonly="1"/>
                            <field name="product_id" 
                                   placeholder="Produit spécifique (optionnel)"
                                   invisible="not product_type"
                                   options="{'no_create': True}"/>
                            <field name="tonnage" class="fw-bold text-primary"/>
                        </group>
                        <group string="🚢 Transport">
                            <field name="vessel_id" 
                                   placeholder="Sélectionner un navire..."
                                   options="{'no_create_edit': True}"/>
                            <field name="pod" placeholder="Ex: HAMBURG, ROTTERDAM..."/>
                            <field name="container_size" widget="radio" options="{'horizontal': true}"/>
                            <field name="booking_number" placeholder="Ex: BK123456"/>
                        </group>
                    </group>
                    
                    <separator string="📝 Notes"/>
                    <field name="note" nolabel="1" placeholder="Notes ou instructions particulières..." widget="text"/>
                </sheet>
                
                <footer>
                    <button name="action_create_ot" 
                            type="object" 
                            string="✅ Créer l'OT" 
                            class="btn-primary"
                            invisible="not formule_id"/>
                    <button name="action_create_and_new" 
                            type="object" 
                            string="➕ Créer et Nouveau" 
                            class="btn-secondary"
                            invisible="not formule_id"
                            title="Créer cet OT et ouvrir un nouveau formulaire pour en créer un autre"/>
                    <button name="action_view_ot" 
                            type="object" 
                            string="👁 Créer et Voir" 
                            class="btn-secondary"
                            invisible="not formule_id"
                            title="Créer cet OT et ouvrir sa fiche"/>
                    <button string="Annuler" class="btn-light" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action to open the wizard -->
    <record id="action_potting_create_ot_wizard" model="ir.actions.act_window">
        <field name="name">Créer un OT</field>
        <field name="res_model">potting.create.ot.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="model_potting_customer_order"/>
        <field name="binding_view_types">form</field>
    </record>
</odoo>
